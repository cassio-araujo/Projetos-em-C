#include <stdio.h>
main()
{
    int contador;

    for (contador = 1; contador <= 1000; contador = contador + 1)
    {
        printf("Numero %d\n", contador);
    }
    system("pause");
    system("cls");
}